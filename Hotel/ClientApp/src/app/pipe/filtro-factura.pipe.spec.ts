import { FiltroFacturaPipe } from './filtro-factura.pipe';

describe('FiltroFacturaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroFacturaPipe();
    expect(pipe).toBeTruthy();
  });
});
