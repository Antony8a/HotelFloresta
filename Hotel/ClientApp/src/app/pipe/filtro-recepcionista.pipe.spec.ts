import { FiltroRecepcionistaPipe } from './filtro-recepcionista.pipe';

describe('FiltroRecepcionistaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroRecepcionistaPipe();
    expect(pipe).toBeTruthy();
  });
});
